name: my_fin
description: A new Flutter project.

publish_to:

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_iconly: ^1.0.2
  font_awesome_flutter: ^9.1.0
  http: ^0.13.3
  fluttertoast: ^8.0.8
  intl: ^0.17.0
  sqflite: ^2.0.0+3
  shared_preferences: ^2.0.6
  path_provider: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/

  fonts:
    - family: Circula
      fonts:
        - asset: assets/fonts/Circular Std Font.otf
